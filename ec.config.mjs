// ABOUTME: Expressive Code configuration for warm light syntax highlighting.
// ABOUTME: Uses a subdued palette that fits the Emacs-inspired site theme.

import { defineEcConfig } from '@astrojs/starlight/expressive-code';

export default defineEcConfig({
  themes: ['gruvbox-light-soft'],
});
